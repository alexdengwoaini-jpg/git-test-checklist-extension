# 🚀 快速开始指南

这是最简单、最快速的安装和使用指南！

## ⚡ 5 分钟快速安装

### Windows 用户（推荐）

1. **双击运行安装脚本**
   ```
   双击: vscode-extension\快速安装.bat
   ```

2. **等待自动完成**
   - 安装依赖
   - 打包扩展
   - 安装到 VS Code

3. **重启 VS Code**

完成！🎉

### 手动安装（所有平台）

```bash
# 1. 进入扩展目录
cd vscode-extension

# 2. 安装依赖
npm install

# 3. 打包扩展
npm run package

# 4. 安装（两种方式任选一种）

# 方式 A: 命令行安装
code --install-extension git-test-checklist-1.0.0.vsix

# 方式 B: VS Code 内安装
# - 打开 VS Code
# - Ctrl+Shift+P
# - 输入 "Install from VSIX"
# - 选择生成的 .vsix 文件
```

## 🎯 首次使用

### 1. 打开项目
```
在 VS Code 中打开包含 git-summary.ps1 的项目目录
```

### 2. 运行命令
```
Ctrl+Shift+P → 输入 "Git 测试" → 选择 "生成测试清单 - 最近10次提交"
```

### 3. 查看结果
```
自动打开 test-checklist.md 文件
```

就这么简单！✨

## 💡 三种使用方式

### 方式 1: 命令面板（最常用）
```
1. Ctrl+Shift+P
2. 输入: Git 测试
3. 选择命令
```

**可用命令**:
- 生成测试清单 - 最近10次提交 ⚡
- 生成测试清单 - 最近20次提交
- 生成测试清单 - 最近30次提交
- 生成 Git 测试清单（自定义数量）
- 查看 Git 提交历史图
- 打开测试清单
- 查看提交详情

### 方式 2: 侧边栏（最直观）
```
1. 点击活动栏的 ✅ 图标
2. 在 "快速操作" 中选择
3. 或在 "最近提交" 中点击提交查看详情
```

### 方式 3: Source Control 面板
```
1. 打开 Source Control (Ctrl+Shift+G)
2. 点击标题栏的 ✅ 按钮
```

## 📋 生成的内容

运行后会生成 `test-checklist.md`，包含：

```markdown
# Git Commit Summary and Test Checklist

## Commit Overview
### Commit #1: fix bug in user service
- Hash: 48cce7a
- Author: ted
- Date: 2 days ago
Changed files:
- SysUserService.cs

## Test Checklist
### Test Item #1
- [ ] Verify bug fix is working
- [ ] Test edge cases
- [ ] Test business logic

### General Regression Tests
- [ ] Core functionality
- [ ] User authentication
- [ ] API validation
```

## ⚙️ 常用配置

打开设置 (`Ctrl+,`)，搜索 "Git 测试清单":

```json
{
  // 默认分析的提交数量
  "gitTestChecklist.defaultCommitCount": 10,
  
  // 输出文件名
  "gitTestChecklist.outputFileName": "test-checklist.md",
  
  // 生成后自动打开
  "gitTestChecklist.autoOpenAfterGenerate": true
}
```

## 🔍 查看详细变更

在生成的清单中，每个提交都有哈希值，可以：

### 方法 1: 使用扩展命令
```
Ctrl+Shift+P → "Git 测试: 查看提交详情" → 输入哈希值
```

### 方法 2: 使用 Git 命令
```bash
git show 48cce7a
```

### 方法 3: 点击侧边栏
```
侧边栏 → 最近提交 → 点击任一提交
```

## 🎨 界面预览

```
┌─ 活动栏 ─────────────────────────┐
│  📁 文件                         │
│  🔍 搜索                         │
│  🔌 Source Control               │
│  🐛 调试                         │
│  ✅ Git 测试清单 ← 点这里        │
└──────────────────────────────────┘

┌─ 侧边栏: Git 测试清单 ───────────┐
│                                   │
│  快速操作                         │
│  ├─ 生成测试清单 (10次)          │
│  ├─ 生成测试清单 (20次)          │
│  ├─ 生成测试清单 (30次)          │
│  ├─ 自定义数量...                │
│  ├─ 查看提交历史图               │
│  └─ 打开测试清单                 │
│                                   │
│  最近提交                         │
│  ├─ 📝 fix bug (2 days ago)      │
│  ├─ 📝 update service (3 days)   │
│  └─ 📝 add feature (5 days)      │
│                                   │
└───────────────────────────────────┘
```

## 🚨 故障排除

### 问题 1: 扩展未显示
**解决**: 重启 VS Code

### 问题 2: 命令不工作
**检查**:
- ✅ 项目中是否有 `git-summary.ps1` 文件
- ✅ 是否在 Git 仓库中
- ✅ PowerShell 是否可用

### 问题 3: 打包失败
**解决**:
```bash
# 全局安装 vsce
npm install -g @vscode/vsce

# 重试打包
vsce package
```

### 问题 4: 安装失败
**手动安装**:
1. 找到生成的 `.vsix` 文件
2. VS Code → Ctrl+Shift+P
3. "Extensions: Install from VSIX..."
4. 选择 `.vsix` 文件

## 📚 下一步

- 📖 阅读 [完整文档](README.md)
- 🔧 查看 [安装指南](INSTALL.md)
- 📝 查看 [更新日志](CHANGELOG.md)
- 🎨 添加 [自定义图标](images/README.md)

## 💬 需要帮助？

1. 检查 [故障排除](#🚨-故障排除) 部分
2. 查看 [完整文档](README.md)
3. 提交 Issue

## 🎉 完成！

现在你可以：
- ✅ 自动生成测试清单
- ✅ 查看提交历史
- ✅ 分析代码变更
- ✅ 提高测试覆盖率

享受高效的测试管理体验！🚀
